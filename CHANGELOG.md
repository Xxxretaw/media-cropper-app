# 更新记录

## 0.5.4 - 2026-08-04

- 黑边检测调整为适度激进：多窗口边界改用中位数汇总，降低小黑边被判定为无黑边的阈值，并将安全内缩调整为 3–5 像素。

## 0.5.3 - 2026-07-30

- 修复固定比例裁切框仅响应横向拖动的问题；新算法综合二维指针位移，并在边界约束下保持对角锚点与目标比例。
- 将可变帧率视频的 FFprobe 帧索引从完整 JSON 缓冲与克隆改为 CSV 流式解析，并增加 200 万帧安全上限，超限后回退为近似帧模式。
- 新增固定比例纵向拖动、边界锚点和帧时间 CSV 解析回归测试。

## 0.5.2 - 2026-07-29

- 黑边检测结果改为向有效画面内多裁 2–4 像素，避免导出画面残留细黑边。
- 缩小主时间轴裁切端点，并让播放游标在与端点重合时优先接收中心区域拖动，避免误改裁切范围。
- 修复视频优化区域被输出片段长文本撑宽，导致检测按钮和时间信息横向溢出的问题。
- 新导入视频默认使用自由画幅和 100% 缩放，保留完整原始画面，不再自动套用 9:16 裁切。
- 将文件入口统一为“添加素材”和“选择素材”，支持一次混选图片与视频并自动分流到对应页签。

## 0.5.1 - 2026-07-29

- 将视频裁剪交互简化为单一主时间轴；拖动起点或终点后自动显示对应端点的逐帧微调区，移除需要手动维护的概览/精细双轨、缩放和平移控制。

## 0.5.0 - 2026-07-29

- 将视频时间范围改为“全片概览 + 可缩放精细轨道”，统一播放游标、裁剪端点和预览状态，并让播放只覆盖选中片段。
- 新增可变帧率视频的真实帧时间索引；恒定帧率改用 FFprobe 有理数帧率，长视频和 29.97 fps 素材不再因浮点换算累积偏差。
- 起止点使用包含语义，内部以结束后一帧作为导出边界；支持单帧片段、逐帧键盘微调和超过一小时的时间码。

## 0.4.4 - 2026-07-20

- 精简视频时间显示：预览时间轴只显示分秒，片段时间轴改为分秒帧，右侧输出片段只保留分秒帧范围，并将 FPS 独立显示在标题右侧。
- 优化 GitHub Release 工作流：前端、Rust 和依赖审计改为并行门禁，Rust 测试改用 Release 配置，并让正式打包复用已验证的编译缓存；完整测试、Clippy、依赖审计、FFmpeg 校验和更新签名均继续保留。

## 0.4.3 - 2026-07-20

- 黑边检测结果现在可针对单个视频撤销并重新应用；撤销后会恢复检测前的画幅、锚点、缩放和裁切框，同时解除黑边区域对后续裁切的限制。
- “跳过黑边检测”调整为“当前视频不参与批量检测”，仅影响“检测全部”，不再与已应用的检测结果混用；当前视频仍可单独检测。
- FFprobe 探测结果新增帧率和总帧数，视频预览与输出片段时间轴改为逐帧步进，并显示毫秒时间、起止帧、选中帧数和 FPS。
- 视频导出起点与时长提高到六位小数精度，并允许导出最短一帧的片段。

## 0.4.2 - 2026-07-20

- 修复右侧工作区展开“视频优化”后内容被压缩裁切、无法滚动查看的问题。
- 黑边检测改为导入后默认不自动执行，并支持逐个视频跳过检测；“检测全部”会自动排除已跳过的视频。

## 0.4.1 - 2026-07-17

- 将内置 FFmpeg/FFprobe 升级至 8.1.2，修复 CVE-2026-8461 与 CVE-2026-30999。
- 视频预览优先直接读取兼容的 H.264 MP4/MOV，代理视频增加单文件 512 MB、缓存总量 1 GB 的硬限制。
- 新增后端媒体任务管理：同素材预览自动取消旧进程，移除素材、清空队列、应用退出和安装更新时终止相关 FFmpeg 任务。
- 图片预览限制最长边为 1600 像素，降低高分辨率素材造成的 IPC 与内存压力。
- 导出改为同目录临时文件写入并在成功后原子替换，失败时不再破坏已有文件。
- 批量图片导出现在遵循每个素材设置的图片格式与质量。
- 升级 Rust XML 与错误处理依赖，清除当前 RustSec 漏洞和未定义行为告警。
- 发布工作流固定第三方 Action 版本，并增加前端测试、Rust 测试、Clippy、依赖审计和 FFmpeg 版本门禁。

## 0.4.0 - 2026-07-17

- 新增应用内自动更新：启动后自动检查 GitHub Releases，支持更新说明、下载进度、签名校验、安装和自动重启。
- 更新包使用独立 Tauri 密钥签名，macOS 应用使用无需 Apple Developer ID 的 ad-hoc 签名。
- 新增标签触发的 GitHub Actions 发布流程，自动生成 DMG、更新包、签名和 `latest.json`。

## 0.3.0 - 2026-07-17

- 重整右侧工作区的信息层级：合并裁切设置、折叠低频与视频优化项、压缩命名预览，并将导出操作固定在底部；无素材时改为简洁空状态。
- 修复视频成功启用代理播放后左侧缩略图退化为 `VID` 占位符的问题，视频封面帧与中间播放预览改为独立状态。
- 新增逐素材导出命名：原始文件保持不变，导出名仅使用可编辑的素材名称；只有同名素材才自动追加三位序号。
- 修复自由比例模式下缩放滑杆只更新数值、未同步缩放中间裁切框的问题；手动调整自由画幅后缩放百分比也会同步更新。
- 新增 Finder 文件拖拽导入，图片和视频可混合拖入并自动分流到对应队列。
- 修复快速切换素材或连续请求预览时，旧请求覆盖当前素材状态的问题。
- 修复代理视频已生成但桌面端无法读取的问题，并补齐素材移除、队列清空、应用退出和异常遗留场景的临时文件清理。
- 队列文件名改为安全的 DOM 文本渲染，并启用内容安全策略。
- 移除未使用的 opener 权限与依赖，缩小桌面端权限范围。
- 将媒体数据模型、文件路由、队列渲染、文件拖拽和裁切框交互从主入口拆分为独立模块。
- 新增混合文件路由单元测试。

## 0.2.0 - 2026-07-10

- 新增视频黑边多时间点自动检测。
- 新增“检测当前”“检测全部”和导入后自动检测。
- 检测结果按视频独立保存并复用于批量导出。
- 新增置信度、动态画幅复核和手动调整状态。
- 画幅比例、锚点、缩放和拖拽现在会在去黑边后的有效区域内继续裁切。
- 修复高分辨率代理预览与原视频裁切坐标不一致的问题。
- 支持带 90°/270° 旋转元数据的视频检测和裁切。
- 将 FFmpeg 探测、预览、检测和导出任务移出界面主线程。
- 批量导出遇到同名文件时自动追加 `_2`、`_3`，不再覆盖已有文件。
- 视频预览与导出改用 macOS VideoToolbox H.264 编码。
- 内置可独立运行的 FFmpeg/FFprobe 8.1.1，不再依赖用户电脑上的 Homebrew。
- 当前 macOS 发行包支持 Apple Silicon 与 macOS 11 及以上版本。
