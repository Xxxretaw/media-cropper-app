# Media Cropper 用户可感知更新汇总

这份记录只保留会直接影响使用方式、界面体验、处理结果或设备资源占用的更新。内部重构、测试流程和开发依赖调整不在此列。

## 0.5.4 - 2026-08-04

- 黑边检测现在会更积极地裁掉边缘黑色区域：综合多个时间点时不再只取最小边界，小黑边也不再轻易被判定为无黑边，同时保留多时间点一致性检查以避免过度裁切。

## 0.5.3 - 2026-07-30

- 固定比例裁切现在会同时响应横向和纵向拖动，并在触碰画面边界时保持对角锚点稳定。
- 可变帧率视频改为流式建立逐帧索引，长视频导入时不再一次性加载和复制全部帧 JSON；极长视频超过安全上限时会自动回退为近似帧模式。

## 0.5.2 - 2026-07-29

- 黑边检测后会向有效画面内多裁 2–4 像素，优先确保导出画面不残留细黑边。
- 主时间轴裁切端点变得更紧凑；与播放游标重合时，拖动中心会优先移动播放位置，仍可从端点两侧调整裁切范围。
- 视频优化区域会在侧栏内正常收缩，检测按钮与较长的输出片段时间不再被裁掉。
- 新导入视频默认保留完整原始画面；只有主动选择 9:16、16:9 等比例时才会进行画幅裁切。
- 导入入口统一为“添加素材”和“选择素材”，现在可以一次选择图片和视频，应用会自动分类到对应页签。

## 0.5.1 - 2026-07-29

- 视频裁剪现在只使用一条主时间轴。拖动起点或终点后会自动打开该端点的逐帧微调区，完成后即可收起，不再需要理解和操作两条时间轴。

## 0.5.0 - 2026-07-29

- 长视频新增“全片概览 + 可缩放精细轨道”：可先在全片中定位，再放大到局部逐帧调整起点、终点和播放位置。
- 播放进度、裁剪范围和预览画面使用同一帧位置；播放只覆盖选中片段，并在最后一个选中帧停止。
- 手机视频、录屏等可变帧率素材会在后台建立真实帧索引；索引失败时会明确提示当前只能近似到帧。
- 起点、终点支持方向键逐帧微调，按住 Shift 可一次移动十帧；最短可选择单帧片段。

## 0.4.4 - 2026-07-20

- 精简视频时间信息：预览时间轴只显示分秒，片段时间轴显示分秒帧，右侧输出片段只显示起止范围；视频 FPS 改为标题右侧的小字，不再重复展示帧序号和总帧数。

## 0.4.3 - 2026-07-20

- 已检测的黑边结果可以针对单个视频“撤销检测结果”，裁切会恢复到检测前的状态，并可重新进入完整画面自由裁切；需要时也能“重新应用检测结果”。
- “当前视频不参与批量检测”只控制“检测全部”，不会锁定或清除已有结果；即使已勾选，仍可使用“检测当前”单独处理该视频。
- 视频片段改为逐帧选择，界面会同时显示毫秒时间、起止帧、选中帧数和视频 FPS；使用方向键可每次微调一帧，并支持最短一帧的导出片段。

## 0.4.2 - 2026-07-20

- 修复“视频优化”展开后右侧面板无法继续向下滚动、部分裁切功能不可见的问题。
- 导入视频后默认不再自动检测黑边；批量处理前可为任意视频勾选“跳过黑边检测”，“检测全部”只处理其余视频。

## 0.4.1 - 2026-07-17

- 优化视频预览：兼容的 H.264 MP4/MOV 可直接预览，不再重复生成代理文件；其他视频的代理文件会限制体积和缓存总量，长视频更不容易占满磁盘。
- 优化高分辨率图片预览，降低打开大图时的内存占用和界面卡顿。
- 移除素材、清空队列、退出应用或安装更新时，会停止尚未结束的媒体处理任务，减少后台残留进程和持续占用资源的情况。
- 导出改为先生成完整临时文件、成功后再替换目标文件；导出失败或被中断时，不会破坏原来已有的文件。
- 批量导出图片时，会分别遵循每个素材设置的图片格式和质量，不再统一按固定格式输出。

## 0.4.0 - 2026-07-17

- 新增应用内自动更新。启动后发现新版本时，可以直接查看更新说明、下载进度并完成安装与重启，不需要反复手动下载 DMG 安装包。

## 0.3.0 - 2026-07-17

- 重整右侧工作区：合并裁切设置、折叠低频选项、压缩命名预览，并将导出操作固定在底部，素材较多时更容易找到当前操作入口。
- 支持为每个素材单独设置导出名称，原始文件名保持不变；只有多个素材名称相同时，才会自动追加三位序号。
- 支持直接从 Finder 拖入文件；图片和视频可以混合拖入，并自动进入各自的处理队列。
- 修复自由模式缩放与中间裁切框不同步的问题，滑杆、数值和实际画幅现在会同步变化。
- 修复视频缩略图退化为 `VID` 占位符的问题，队列封面与中间视频预览可以分别正常显示。
- 优化快速切换素材和连续请求预览时的稳定性，减少旧画面覆盖当前素材的情况。
- 完善视频代理文件清理：移除素材、清空队列、正常退出或异常退出后的下次启动都会进行对应清理，避免临时文件长期堆积。

## 0.2.0 - 2026-07-10

- 新增视频黑边自动检测，支持导入后自动检测、“检测当前”和“检测全部”。
- 每个视频会独立保存黑边检测结果，并在批量导出时继续使用；遇到动态画幅或置信度不足时会提示人工确认。
- 检测黑边后，画幅比例、锚点、缩放和手动拖拽都会限制在有效画面内，避免重新把黑边带回导出结果。
- 支持带 90°/270° 旋转信息的视频，预览裁切框和最终导出位置保持一致。
- 视频探测、预览、黑边检测和导出转到后台执行，并使用 macOS 硬件编码，处理期间界面更流畅。
- 批量导出遇到同名文件时自动追加 `_2`、`_3`，不会覆盖目录中已有的文件。
- 应用内置 FFmpeg/FFprobe，不再要求用户提前安装 Homebrew 或单独配置媒体工具。
- 支持 Apple Silicon Mac，最低系统版本为 macOS 11。
